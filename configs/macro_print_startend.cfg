#####################################################################
# 	Print start/end and related macro's
#####################################################################

[gcode_macro PRINT_START]
##  Z offset added after heating to print temp, to compensate for hotend
##  thermal expansion between the contact-probing temp and print temp.
##  The nozzle grows TOWARD the bed as it heats, so the compensation is
##  positive: it lifts the nozzle back up. Leaving this at 0 therefore
##  prints slightly closer than nominal, never further away - it cannot
##  cause a first layer that refuses to stick, and raising it would make
##  one worse. Beacon's docs quote ~0.06 for a 150C -> print temp swing,
##  but calibrate a real coefficient (mm per degree C) rather than guess;
##  see https://github.com/YanceyA/BeaconPrinterTools thermal expansion tool.
##  Keep at 0 until the contact tap is proven repeatable and the first
##  layer sticks, then calibrate.
variable_thermal_expansion_z_per_degree: 0.0
variable_thermal_expansion_probe_temp: 150
gcode:
    # This part fetches data from your slicer. Such as bed temp, extruder temp, chamber temp and size of your printer.
    {% set target_bed = params.BED|int %}
    {% set target_extruder = params.EXTRUDER|int %}
    {% set target_chamber = params.CHAMBER|default("40")|int %}
    {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
    {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}

    BED_MESH_CLEAR
    SET_GCODE_OFFSET Z=0

    {% if printer.extruder.temperature > 175 %}
        SET_DISPLAY_TEXT MSG="Cooling hotend before homing"
        TEMPERATURE_WAIT SENSOR=extruder MAXIMUM=175    ; contact homing needs to be below contact_max_hotend_temperature
    {% endif %}

    G28     ; home axes
    G0 Z2   ; position beacon at 2mm for heat soak
    
    STATUS_HEATING
    SET_FAN_SPEED FAN=filter SPEED=0.6
    M140 S{target_bed}                      ; start bed heater
    M109 S{thermal_expansion_probe_temp}    ; preheat nozzle to probing temp
    M190 S{target_bed}                      ; wait on bed temperature

    ##  Clean at print temperature, not at probing temperature. The brush can
    ##  only take off residue that is molten, and clean_nozzle skips its purge
    ##  below purge_temp_min. A bead left on the tip trips the contact probe
    ##  early, zeroing Z too high and giving a first layer that will not stick.
    ##  Park over the bucket first so the ooze from heating lands there.
    {% set cn = printer["gcode_macro clean_nozzle"] %}
    SET_DISPLAY_TEXT MSG="Heating nozzle to clean"
    G90
    G1 Z{cn.brush_top + cn.clearance_z} F{cn.prep_spd_z}
    G1 Y{printer.toolhead.axis_maximum.y} F{cn.prep_spd_xy}
    G1 X{cn.bucket_start + (cn.bucket_left_width / 2)} F{cn.prep_spd_xy}
    M109 S{target_extruder}
    CLEAN_NOZZLE                            ; purge + wipe with molten filament

    ##  Back below contact_max_hotend_temperature before tapping. The bed soak
    ##  runs during the cooldown, so this costs no extra time.
    SET_DISPLAY_TEXT MSG="Soak and cool for contact probe"
    M104 S{thermal_expansion_probe_temp}
    G4 P300000                              ; let the frame settle for 5 min
    TEMPERATURE_WAIT SENSOR=extruder MAXIMUM={thermal_expansion_probe_temp}
    
    SET_DISPLAY_TEXT MSG="Beacon calibrate contact"      # Displays info

    G28 Z METHOD=CONTACT CALIBRATE=1    ; calibrate z offset and beacon model hot

    SET_DISPLAY_TEXT MSG="QGL"      # Displays info
    STATUS_LEVELING                 # Sets SB-leds to leveling-mode
    quad_gantry_level
    
    SET_DISPLAY_TEXT MSG="Bed mesh"    # Displays info
    STATUS_MESHING
    BED_MESH_CALIBRATE RUNS=2           ; bed mesh in scan mode

    CLEAN_NOZZLE
    SET_DISPLAY_TEXT MSG="Beacon calibrate contact"      # Displays info

    G28 Z METHOD=CONTACT CALIBRATE=0    ; calibrate z offset only after tilt/mesh
    
    SET_DISPLAY_TEXT MSG="Heating nozzle"      # Displays info

    M104 S{target_extruder}                    ; set extruder to print temp
    M109 S{target_extruder      }                    ; wait for extruder temp

    {% set expansion = (target_extruder - thermal_expansion_probe_temp) * thermal_expansion_z_per_degree %}
    SET_GCODE_OFFSET Z={expansion}    ; compensate for hotend thermal expansion above probe temp

    SET_DISPLAY_TEXT MSG="Printer goes brr"          # Displays info

    STATUS_PRINTING
    LINE_PURGE    ; call another macro to purge or prime nozzle

[gcode_macro PRINT_END]
#   Use PRINT_END for the slicer ending script - please customize for your slicer of choice
gcode:
    M400                           ; wait for buffer to clear
    G92 E0                         ; zero the extruder
    G1 E-5.0 F3600                 ; retract filament
    G91                            ; relative positioning
    SET_FAN_SPEED FAN=filter SPEED=0

    #   Get Boundaries
    {% set max_x = printer.configfile.config["stepper_x"]["position_max"]|float %}
    {% set max_y = printer.configfile.config["stepper_y"]["position_max"]|float %}
    {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}

    #   Check end position to determine safe direction to move
    {% if printer.toolhead.position.x < (max_x - 20) %}
        {% set x_safe = 20.0 %}
    {% else %}
        {% set x_safe = -20.0 %}
    {% endif %}

    {% if printer.toolhead.position.y < (max_y - 20) %}
        {% set y_safe = 20.0 %}
    {% else %}
        {% set y_safe = -20.0 %}
    {% endif %}

    {% if printer.toolhead.position.z < (max_z - 2) %}
        {% set z_safe = 2.0 %}
    {% else %}
        {% set z_safe = max_z - printer.toolhead.position.z %}
    {% endif %}

    {% set z_park_abs = printer.toolhead.position.z + 25.0 %}
    {% if z_park_abs > max_z %}
        {% set z_park_abs = max_z %}
    {% endif %}

    G0 Z{z_safe} F3600               ; move nozzle up
    G0 X{x_safe} Y{y_safe} F20000    ; move nozzle to remove stringing

    TURN_OFF_HEATERS
    SET_FAN_SPEED FAN=filter SPEED=0

    BED_MESH_CLEAR
    M107                             ; turn off fan
    G90                              ; absolute positioning
    G0 Z{z_park_abs} F3600           ; move nozzle up away from the print
    G0 X{max_x / 2} Y{max_y} F15000  ; park nozzle at rear
    STATUS_READY
    M117 Done


